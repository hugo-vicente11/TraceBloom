{
  "name": "@tracebloom/eval",
  "version": "0.1.0",
  "description": "TraceBloom evaluation framework: pluggable evaluators (deterministic + LLM-as-judge) producing OpenTelemetry gen_ai.evaluation.result-shaped outcomes.",
  "license": "Apache-2.0",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "@tracebloom/sdk": "workspace:*",
    "ajv": "^8.20.0",
    "ajv-formats": "^3.0.1"
  },
  "devDependencies": {
    "@types/node": "^26.1.2",
    "typescript": "^6.0.0",
    "vitest": "^4.1.9"
  }
}
